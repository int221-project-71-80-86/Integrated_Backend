services:
  backend:
    build: .
    image: backend:latest
    ports:
      - "8083:8083"
    networks:
      test:
        ipv4_address: 10.5.0.11
networks:
  test:
    external:
      name: testNetwork2